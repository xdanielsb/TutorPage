//Create the project django
	django-admin startproject myproject

//Create the app 
	python manage.py start myapp_name 

//add to settings the app in installed apps put the name of the app

//activate migratios off the app
	python manage.py makemigrations myapp_name

//init the app 
	 python manage.py runserver

//in the urls file add the main method- regular expressions
    url(r'^$', 'mypage.views.home', name='home'), 

//Cuando cree un subfolder
  crear el archive __init__ para que identifique el programa que es un subfolder
